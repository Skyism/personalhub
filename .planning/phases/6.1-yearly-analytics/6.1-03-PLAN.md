---
phase: 6.1-yearly-analytics
plan: 03
type: execute
---

<objective>
Add entry point from main analytics page to yearly view and ensure navigation/empty state polish.

Purpose: Users can discover and reach yearly analytics from Finance → Analytics.
Output: Link on /finance/analytics to "View yearly spending" (or "Yearly") pointing to /finance/analytics/yearly. Yearly page back link/breadcrumb to analytics. Empty state copy and layout polish.
</objective>

<context>
@.planning/yearly-analytics-prd.md
@app/finance/analytics/page.tsx
@app/finance/analytics/yearly/page.tsx
@components/navigation/TopAppBar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Link from analytics to yearly view</name>
  <files>app/finance/analytics/page.tsx</files>
  <action>
On the main analytics page (/finance/analytics), add a clear link to the yearly view:
- Place near the header (e.g. next to or below "Analytics" title, or beside budget selector). Use Link to /finance/analytics/yearly. Label: "View yearly spending" or "Yearly".
- Style consistent with existing links (e.g. text-primary or link style). Do not remove or replace the budget selector.
</action>
  <verify>From /finance/analytics user can click to /finance/analytics/yearly</verify>
</task>

<task type="auto">
  <name>Task 2: Yearly page back navigation</name>
  <files>app/finance/analytics/yearly/page.tsx</files>
  <action>
Ensure yearly page has explicit back navigation to analytics: TopAppBar fallbackHref="/finance/analytics" (already in 6.1-01). Add a "Back to Analytics" or "← Analytics" link at top of content for consistency with other Finance pages.
</action>
  <verify>From yearly page user can return to /finance/analytics</verify>
</task>

<task type="auto">
  <name>Task 3: Empty state and copy polish</name>
  <files>app/finance/analytics/yearly/page.tsx</files>
  <action>
- Empty state: When selected year has no transactions, message "No transactions in {year}" (already in 6.1-01). Ensure chart section is hidden or shows friendly empty state in that case.
- Ensure currency formatting uses same pattern as rest of app (e.g. Intl.NumberFormat USD). Ensure mobile layout: padding, card stacking, touch targets consistent with analytics and budget pages.
</action>
  <verify>Empty state clear; currency and layout consistent</verify>
</task>

</tasks>

<verification>
- [ ] Link from /finance/analytics to yearly exists and is visible
- [ ] Yearly page has back link to analytics
- [ ] Empty state and formatting consistent
</verification>
