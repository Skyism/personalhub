---
phase: 6.1-yearly-analytics
plan: 02
type: execute
---

<objective>
Add one spending chart to the yearly analytics page: monthly total (Jan–Dec) for the selected year.

Purpose: Visualize spending trend across the year.
Output: Bar or line chart with 12 months on X-axis and total spent per month on Y-axis. Reuse ChartWrapper and Recharts patterns; responsive.
</objective>

<context>
@.planning/yearly-analytics-prd.md
@components/charts/ChartWrapper.tsx
@components/charts/SpendingTrendsChart.tsx
@app/finance/analytics/yearly/page.tsx

**Data:** Monthly series from 6.1-01: for selected year, GROUP BY month (1–12), SUM(amount). Pass to chart as array of { month: number, monthLabel: string, amount: number }.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Monthly spending chart component and integration</name>
  <files>components/charts/YearlyMonthlyChart.tsx, app/finance/analytics/yearly/page.tsx</files>
  <action>
1. **Compute monthly series in yearly page:** From filtered transactions (by year), group by month (1–12), sum amount. Build array of 12 points: { month: 1..12, monthLabel: "Jan".."Dec", amount: number }. Months with no transactions have amount 0.

2. **Create YearlyMonthlyChart:** Client component under components/charts/. Props: data: { month: number, monthLabel: string, amount: number }[]. Use Recharts BarChart or LineChart. ChartWrapper height 300. XAxis: monthLabel. YAxis: format as currency. Tooltip: currency. Match styling (var(--chart-1), var(--border), var(--muted-foreground)) from SpendingTrendsChart. Empty state: "No data" if all zeros or empty array.

3. **Integrate:** In yearly page, compute monthlyData from transactions and pass to YearlyMonthlyChart (dynamic import or client child). Section title "Spending by month".
</action>
  <verify>Chart shows 12 months; amounts match aggregated monthly totals; responsive; empty/zero state handled</verify>
</task>

</tasks>

<verification>
- [ ] YearlyMonthlyChart renders 12 months with correct amounts
- [ ] Chart is responsive (ChartWrapper)
- [ ] Zero/empty state does not break layout
</verification>
