---
phase: 8.3-trip-budget-tracking
plan: 02
type: execute
---

<objective>
Extend SMS webhook to support trip transactions via "trip" keyword prefix, with automatic active trip detection and idempotency.

Purpose: Enable SMS-based trip transaction logging using the established Twilio integration, allowing users to text "trip 25 dinner" to create trip transactions.
Output: Updated SMS parser and webhook that routes trip transactions to wants_trip_transactions table with proper trip and budget association.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/8.1-semi-annual-wants-budget/8.1-02-SUMMARY.md
@.planning/phases/8.3-trip-budget-tracking/8.3-01-SUMMARY.md
@app/api/sms/route.ts
@lib/twilio/parser.ts
@lib/wants/queries.ts

**Tech stack available:**
- Twilio SDK v5.11.2 (Phase 5)
- SMS webhook with signature validation (Phase 5)
- Wants budget schema with period tracking (Phase 8.1)
- Trip schema with active trip tracking (Phase 8.3 Plan 1)

**Established patterns:**
- SMS format: "amount note" creates regular transaction (Phase 5)
- "wants amount note" creates wants transaction (Phase 8.1)
- Webhook validation: signature → idempotency → parse → create → respond (Phase 5)
- Idempotency via twilio_message_id unique constraint (Phase 5)
- Silent success, error SMS only on parse failure (Phase 5)
- Parser returns { amount, note, type } where type is 'regular' | 'wants' (Phase 8.1)

**Constraining decisions:**
- Phase 5: Simplified SMS format prioritizes UX over feature density
- Phase 5: Direct Supabase insert for speed (15-second webhook timeout)
- Phase 8.1: Case-insensitive keyword detection (accepts "wants", "want", "WANTS")
- Phase 8.3 Requirements: "trip" keyword routes to active trip, error if no active trip

**Issues being addressed:** None
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend SMS parser to detect trip transactions</name>
  <files>lib/twilio/parser.ts</files>
  <action>
Update the parseSMS function to detect and parse trip transactions.

Add "trip" keyword detection alongside existing "wants" detection:
- Regex: /^(trips?)\s+/i to match "trip" or "trips" prefix (case-insensitive)
- Strip prefix and parse remainder as "amount note"
- Return type: 'trip' (extend existing union: 'regular' | 'wants' | 'trip')

Detection order: trip → wants → regular (check trip first since "trip" is more specific)

Avoid changing the existing wants or regular parsing logic. Just add the new trip branch.
  </action>
  <verify>Parser correctly identifies "trip 25 dinner" as type 'trip' with amount 25 and note 'dinner'. "trip" and "TRIP" both work. Regular and wants parsing unchanged.</verify>
  <done>Parser detects trip keyword, returns correct type/amount/note, case-insensitive, backward compatible</done>
</task>

<task type="auto">
  <name>Task 2: Add trip query utilities</name>
  <files>lib/wants/queries.ts</files>
  <action>
Add two new functions to the existing queries file:

**findActiveTrip()** - Finds the currently active trip for the user
- Query wants_trips WHERE user_id = TEMP_USER_ID AND status = 'active'
- Return { trip } or { trip: null } if no active trip
- Include wants_budget_id in the returned data for dual-deduction

**createTripTransaction()** - Creates a trip transaction via SMS
- Insert into wants_trip_transactions with:
  - user_id: TEMP_USER_ID
  - trip_id: from active trip
  - wants_budget_id: from active trip's budget association
  - amount, note from parsed SMS
  - source: 'sms'
  - twilio_message_id for idempotency
  - transaction_date: current date

Follow the same pattern as existing createWantsTransaction() function.
  </action>
  <verify>Functions query/insert correctly, TypeScript types match schema</verify>
  <done>findActiveTrip and createTripTransaction functions created, follow established patterns</done>
</task>

<task type="auto">
  <name>Task 3: Add trip routing branch to SMS webhook</name>
  <files>app/api/sms/route.ts</files>
  <action>
Add trip transaction routing to the webhook POST handler.

Insert a new branch AFTER the wants check and BEFORE the regular transaction flow:

```
if (parseResult.type === 'trip') {
  1. Call findActiveTrip()
  2. If no active trip → return error TwiML: "No active trip. Create one in the app first."
  3. Check idempotency: query wants_trip_transactions for existing twilio_message_id
  4. If duplicate → return empty TwiML (silent)
  5. Call createTripTransaction() with trip data + parsed amount/note + MessageSid
  6. Log success
  7. Return empty TwiML (silent success)
}
```

Follow the exact same pattern as the wants routing branch for consistency.
  </action>
  <verify>SMS "trip 25 dinner" creates transaction in wants_trip_transactions linked to active trip. Error message when no active trip. Idempotency works. Regular and wants SMS unaffected.</verify>
  <done>Trip SMS routing working end-to-end, error handling for no active trip, idempotency, backward compatible</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build succeeds without TypeScript errors
- [ ] "trip 25 dinner" parsed as type 'trip' with amount 25 and note 'dinner'
- [ ] "TRIP 100 hotel" works (case-insensitive)
- [ ] Regular "50 groceries" and "wants 30 coffee" still work unchanged
- [ ] Webhook routes trip SMS to wants_trip_transactions table
- [ ] Error message returned when no active trip exists
- [ ] Duplicate SMS detection works for trip transactions
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- SMS parser handles all three types: regular, wants, trip
- Webhook correctly routes each type to its respective table
- No breaking changes to existing SMS flows
</success_criteria>

<output>
After completion, create `.planning/phases/8.3-trip-budget-tracking/8.3-02-SUMMARY.md`:

# Phase 8.3 Plan 2: SMS Integration Extension Summary

**[Substantive one-liner - what shipped, not "phase complete"]**

## Accomplishments

- Extended SMS parser to detect "trip"/"trips" keyword prefix (case-insensitive)
- Webhook routes trip transactions to wants_trip_transactions table with active trip association
- Automatic active trip lookup with helpful error messages
- Idempotency support for trip SMS via twilio_message_id unique constraint
- Backward compatible - regular and wants transactions work unchanged

## Files Created/Modified

- `lib/twilio/parser.ts` - Added trip detection to parseSMS function
- `lib/wants/queries.ts` - findActiveTrip() and createTripTransaction() utilities
- `app/api/sms/route.ts` - Extended webhook to route trip transactions

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 8.3-03-PLAN.md (Trip UI - list, create, detail pages)
</output>
